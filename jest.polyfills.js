/**
 * Reserved for globals that must exist before test modules load.
 * Route tests that need Request/Response use `@jest-environment node`
 * (see tests/app/feedback-route.test.ts). Component tests mock fetch directly.
 */
