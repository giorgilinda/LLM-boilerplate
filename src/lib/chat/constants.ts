/** Default system prompt for the chat example. Override per project. */
export const DEFAULT_SYSTEM_PROMPT =
  "You are a helpful, concise assistant. When the user attaches an image, describe what you see and answer questions about it.";

export const IS_MOCK_MODE = process.env.NEXT_PUBLIC_MOCK_MODE === "true";
